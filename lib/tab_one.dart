import 'package:flutter/material.dart';

class TabOneWidget extends StatelessWidget {
  @override
    Widget build(BuildContext context) {
      return new Container(
        color: const Color(0xFF0084BF),
      );
    }
}